/** which control will be used for a given property type */
export enum WidgetViewFormat {
    Boolean = 'boolean',
    NumericInput = 'number',
    Color = 'color',
    TextArea = 'textArea',
    TextInput = 'string',
    Dropdown = 'enum',
    LayoutOptions = 'layoutOptions',
}
